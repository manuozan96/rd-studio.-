import ContactoBack from "@/components/ContactoBack";
import Layout from "../components/Layout";

const Contacto = () => {
  return (
    <Layout show={false}>
      <ContactoBack />
    </Layout>
  );
};

export default Contacto;
